/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Real imagery (public-domain or family material only) goes in /public and is
  // rendered with next/image. No remote image hosts are whitelisted on purpose.
};

export default nextConfig;
